import UserEdit from 'Components/UserEdit';
import { ReactElement } from 'react';

/** Компонент страницы редактирования пользователя */
const UserEditPage = (): ReactElement => {
	return (
		<>
			<UserEdit />
		</>
	);
};

export default UserEditPage;
